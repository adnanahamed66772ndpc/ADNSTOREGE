name = "adnstorge"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[[d1_databases]]
binding = "DB"
database_name = "adnstorge-db"
database_id = "placeholder"

# Run: npm run db:create then paste the database_id above, or set in Cloudflare dashboard for Pages
